# Fake SpeedTest Server

Приватный HTTP-сервер для эмуляции загрузки файлов различных размеров с целью тестирования пропускной способности сетевого канала.

**Версия: 2.13.0**

## История изменений

### Версия 2.13.0 (текущая)
- **Переопределение Console.WriteLine()**: Выполнено переопределение метода Console.WriteLine() через создание класса ConsoleAndFileWriter, который наследуется от TextWriter
  - Все вызовы Console.WriteLine() автоматически пишут текст и в консоль, и в файл логов
  - Добавление временной метки к каждому сообщению
  - Система логирования упрощена - методы Log() и LogErrorToFile() теперь просто используют переопределенный Console.WriteLine()
  - Устранено дублирование кода и обеспечено централизованное управление выводом

### Версия 2.12.0 (предыдущая)
- **Gzip сжатие статики**: Добавлена поддержка gzip-сжатия для статических файлов
  - Автоматическое определение поддержки gzip браузером через заголовок Accept-Encoding
  - Сжатие файлов: index.html, style.css, script.js, admin.html, admin.js
  - Добавление заголовка Content-Encoding: gzip в ответ
  - Уменьшение размера передаваемых данных до 70-80%
- **Антиспам замеров пинга**: Добавлена защита от частых нажатий кнопки измерения пинга
  - Блокировка кнопки на 2 секунды после каждого замера
  - Отображение обратного отсчета при попытке повторного нажатия
  - Защита от параллельных запросов измерения
- **Изменение количества замеров пинга**: Уменьшено с 10 до 3 последовательных запросов
  - Ускорение процесса измерения
  - Сохранение точности за счет использования медианы

### Версия 2.10.0 (предыдущая)
- **Base64url утилита**: Добавлен новый модуль `Base64Url.cs` для кодирования и декодирования в формате Base64URL (RFC 4648 Section 5)
  - Метод `Encode(byte[] data)` - кодирование байтового массива в Base64URL строку
  - Метод `Encode(string text)` - кодирование UTF-8 строки в Base64URL строку
  - Метод `DecodeToBytes(string base64Url)` - декодирование Base64URL строки в байтовый массив
  - Метод `DecodeToString(string base64Url)` - декодирование Base64URL строки в UTF-8 строку
  - Метод `IsValid(string base64Url)` - валидация формата Base64URL строки
  - URL-safe символы: `-` вместо `+`, `_` вместо `/`, без padding-символов `=`

### Версия 2.9.3 (предыдущая)
- **Логирование User-Agent**: Вывод полного User-Agent клиента в консоль и логи без обрезки
- **Оптимизация обработки забаненных клиентов**: 
  - Проверка бана перемещена в начало обработки запроса
  - Для забаненных IP не выполняется парсинг заголовков
  - Мгновенное закрытие соединения без отправки ответа (клиент висит до таймаута)

### Версия 2.9.2 (предыдущая)
- **Синхронизация версий**: Обновлены версии в коде и документах до 2.9.2
- **Рефакторинг кода**: Версия проекта вынесена в глобальную константу `AppVersion` для централизованного управления

### Версия 2.9.1 (предыдущая)
- **Синхронизация версий**: Обновлены версии в коде и документах до 2.9.1

### Версия 2.9 (предыдущая)
- **Исправление безопасности**: Устранена уязвимость Path Traversal (CVE-2024-XXXX)
  - Добавлена валидация путей через `Path.GetFullPath()`
  - Реализована проверка на принадлежность пути к базовой директории приложения
  - Блокировка попыток доступа к файлам вне разрешённой директории (HTTP 403)

### Версия 2.7
- Предыдущий стабильный релиз

## Описание

Сервер имитирует публичные сервисы измерения скорости (аналог speedtest.net), предоставляя возможность загрузки фейковых файлов размером от 1 МБ до 10 ГБ.

## Системные требования

- ОС: Windows Server 2008 R2 SP1 или новее
- Платформа: .NET Framework 4.8 (обязательно)
- Права доступа: Администратор (для запуска на порту 5000)

## Структура проекта

Проект разделен на модули для улучшения читаемости и поддерживаемости:

- `Program.cs` - Точка входа, оркестрация компонентов, логирование
- `BanManager.cs` - Управление банами клиентов и отслеживание запросов
- `NightModeService.cs` - Сервис ночного режима (01:00-06:00)
- `RequestHandler.cs` - Обработка HTTP-запросов, маршрутизация
- `Base64Url.cs` - Утилита для Base64URL кодирования/декодирования (RFC 4648 Section 5)
- `index.html` - Главная страница с интерфейсом теста скорости
- `style.css` - Стили пользовательского интерфейса
- `script.js` - Клиентская логика теста скорости
- `suspicious_agents.txt` - Список подозрительных User-Agent
- `random_headers.txt` - Заголовки для рандомизации X-Powered-By

## Сборка

```bash
msbuild "fake server speedtest.csproj" /p:Configuration=Release
```

## Запуск

```bash
FakeDownloadServer.exe
```

## Доступ

Сервер доступен по адресу: `http://localhost:5000/`

## Функционал

### Генерация файлов

Сервер генерирует файлы "на лету" без сохранения на диск:
- Начало файла содержит маркер: `ТЕСТ` (UTF-8)
- Конец файла содержит маркер: `ТЕСТ` (UTF-8)
- Остальное содержимое — нулевые байты

Доступные эндпоинты:
- `/download/100` - 100 МБ
- `/download/200` - 200 МБ
- `/download/500` - 500 МБ
- `/download/1000` - 1000 МБ (1 ГБ)
- `/download/N` - где N от 1 до 10240 МБ

### Система безопасности

#### Белый список URL
- `/` — главная страница
- `/favicon.ico` — иконка сайта
- `/updateBrowserInfo` — обновление информации о браузере
- `/748_dark` — секретный код разбана
- `/download/N` — скачивание файлов

#### Детектирование подозрительных запросов
Бан накладывается автоматически при любом подозрительном запросе:
- Подозрительном User-Agent (содержит ключевые слова из `suspicious_agents.txt`)
- Пустом User-Agent
- Accept: */* без Accept-Language
- Запросе вне белого списка

**Важно:** Второй шанс не предоставляется. Любой подозрительный запрос приводит к немедленной блокировке.

#### Срок блокировки
- Срок блокировки: 10 лет
- Файл хранения: `bans.ini`
- Формат: `IP_адрес|YYYY-MM-DD HH:mm:ss`

#### Секретный код разбана
Код `748_dark` может быть передан:
- В URL: `http://server:5000/748_dark`
- В Query String: `http://server:5000/?748_dark`
- В User-Agent: любой, содержащий `748_dark`

### Ночной режим

По умолчанию включён с 01:00 до 06:00:
- Сервер переходит в режим ожидания
- Обработка запросов приостанавливается
- Можно нажать **'Y'** в консоли для принудительного запуска

### Логирование

- Формат имени: `server_log_YYYY-MM-DD_HH-mm-ss.txt`
- Кодировка: UTF-8
- Создаётся новый файл при каждом запуске

### Статистика подключений

В заголовке окна приложения отображается количество подключений за последние 24 часа.
Обновление происходит каждые 30 секунд.

## Конфигурационные файлы

### suspicious_agents.txt
Список ключевых слов для детектирования ботов (поддерживает горячую перезагрузку).

### random_headers.txt
Список заголовков для рандомизации X-Powered-By.

### bans.ini
Файл хранит забаненные IP-адреса (создаётся автоматически).

## Структура проекта

```
/workspace/
├── Program.cs                  # Основной код сервера
├── BanManager.cs               # Управление банами клиентов
├── NightModeService.cs         # Сервис ночного режима
├── RequestHandler.cs           # Обработка HTTP-запросов
├── Base64Url.cs                # Утилита Base64URL кодирования
├── App.config                  # Конфигурация приложения
├── fake server speedtest.csproj # Файл проекта
├── README.md                   # Этот файл
├── random_headers.txt          # Заголовки для рандомизации
├── suspicious_agents.txt       # Подозрительные User-Agent
├── bans.ini                    # Файл банов (создаётся при работе)
└── server_log_*.txt           # Файлы логов (создаются при работе)
```

## Лицензия

Приватный проект для личного использования в закрытых сетях.
